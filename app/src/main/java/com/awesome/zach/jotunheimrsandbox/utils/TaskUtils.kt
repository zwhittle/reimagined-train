package com.awesome.zach.jotunheimrsandbox.utils

class TaskUtils {
    companion object {
        const val LOG_TAG = "TaskUtils"
    }

    /**
     *  Receives a string input by the user and parses it into a Task.
     *  Returns a Task object
     *
     *  Legend:
     *  # = Tag
     *  $ = Project, inbox if null
     *
     */
    fun parseInputIntoTask(input: String) {

    }
}