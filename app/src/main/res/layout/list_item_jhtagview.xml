<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <import type="android.graphics.Color" />

        <variable name="clickListener" type="android.view.View.OnClickListener"/>

        <variable name="tagText" type="String"/>

    </data>


    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                       xmlns:app="http://schemas.android.com/apk/res-auto"
                                                       xmlns:tools="http://schemas.android.com/tools"
                                                       android:layout_width="wrap_content"
                                                       android:onClick="@{clickListener}"
                                                       android:layout_margin="@dimen/margin_xxs"
                                                       android:layout_height="wrap_content">


        <com.awesome.zach.jotunheimrsandbox.ui.view.JHTagView
                android:layout_width="wrap_content"
                android:id="@+id/jhtagview"
                android:paddingStart="@dimen/margin_small"
                android:paddingEnd="@dimen/margin_small"
                android:paddingTop="@dimen/margin_xxs"
                android:paddingBottom="@dimen/margin_xxs"
                android:textColor="@android:color/white"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="@string/_420_blaze_it_nerds"
                android:text="@{tagText}"
                android:layout_height="wrap_content"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>