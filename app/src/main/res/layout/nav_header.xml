<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/navigation_drawer_header_height"
    android:background="@color/colorAccent"
    android:gravity="center"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/margin_small"
    android:theme="@style/NavHeaderTheme">

    <ImageView
        android:id="@+id/tvNavHeaderImageLeft"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_gravity="bottom|start"
        android:contentDescription="@string/smoke_weed_everyday"
        android:src="@drawable/ic_pot_leaf" />

    <LinearLayout
        android:id="@+id/layoutNavHeaderTextArea"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        android:paddingBottom="15dp">

        <TextView
            android:id="@+id/tvNavHeaderTopText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/_420_blaze_it_nerds"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvNavHeaderBottomText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/smoke_weed_everyday" />
    </LinearLayout>

    <ImageView
        android:id="@+id/tvNavHeaderImageRight"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_gravity="bottom|end"
        android:contentDescription="@string/smoke_weed_everyday"
        android:src="@drawable/ic_pot_leaf" />
</LinearLayout>